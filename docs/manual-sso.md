# Manual SSO Documentation

Use this worksheet for SSO details not returned by API.

- SSO enabled:
- Identity provider type:
- Entity ID / audience:
- Redirect/ACS URLs:
- Attribute mappings (username, email, groups):
- JIT provisioning settings:
- Failback/local auth behavior:
